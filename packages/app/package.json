{
  "name": "@protimer/app",
  "version": "1.0.0",
  "description": "ProTimer desktop app",
  "private": true,
  "scripts": {
    "dev": "cargo tauri dev",
    "build": "cargo tauri build",
    "build:universal": "cargo tauri build --target universal-apple-darwin",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
